Two dimensional labyrinth game

Dark Dimension and Light dimension

Wall and door and key visible only in one dimension arrival as well
Top dawn view
Torch light for viewing not far ahead
switching dimension on key
CRUNCH if switch inside wall
only new game button and time screen result as well a death try again rsult screen
Map data loadable in Json for user to make a map only one map to do for the game submit
-debug option if map loaded to debug the map with the gameplay without the light
two dimensional seeing curent dimension at 1 opacity and other dimension on top of it at 0.1(can change see on test) opacity so if in vision you can see wall for other dimension
Arrival always visible
player always visible

goal 48h dev not full week
